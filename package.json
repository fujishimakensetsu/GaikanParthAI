{
  "name": "archienhance-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static HTML - no build required'",
    "preview": "npx serve ."
  }
}
